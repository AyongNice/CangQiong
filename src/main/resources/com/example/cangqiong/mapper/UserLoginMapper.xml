<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cangqiong.mapper.UserLoginMapper">


    <insert id="addUser">
        insert into user(openid, name, sex, phone, id_number, avatar, create_time)
        values (#{openId}, #{username}, #{sex}, #{phone}, #{idNumber}, #{avatar}, #{createTime})
    </insert>
</mapper>
